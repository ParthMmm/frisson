# Implementation Plans

Generated by the `/improve` skill on 2026-06-30, planned against commit
`32cd2be`. Execute in the order below unless dependencies say otherwise. Each
executor: read the plan fully before starting, honor its STOP conditions,
and update your row when done.

**Note on repo drift**: between this audit and plan-writing session, the
working tree moved: commit `1e29d93` ("Set up OXC tooling") landed, and
`src/routes/+page.svelte` has further uncommitted changes (station-pulse
animation, an Apple Music "no match" cache, selected-station persistence).
None of this was made by this planning session. Every plan below has a
"drift check" step at the top that compares the live code against the
excerpts quoted in the plan — run it first, every time, before trusting the
line numbers in "Current state."

## Execution order & status

| Plan | Title                                                   | Priority | Effort | Depends on | Status |
| ---- | ------------------------------------------------------- | -------- | ------ | ---------- | ------ |
| 001  | Move Radio France API token to a Cloudflare env binding | P1       | S      | —          | DONE   |
| 002  | Validate station params on API routes                   | P1       | S      | 001        | DONE   |
| 003  | Persist favorite stations across reloads                | P2       | S      | —          | DONE   |
| 004  | Clear the transitive `cookie` advisory                  | P3       | S      | —          | DONE   |
| 006  | Remove unreachable guard in `readCachedCurrentTrack`    | P3       | S      | —          | DONE   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 002 depends on 001 only to avoid both plans editing the same lines of
  `src/routes/api/current-track/+server.ts` at once — there's no functional
  coupling, either order would work correctness-wise.
- 003 and 006 both touch `src/routes/+page.svelte`. No hard dependency
  between them, but running 003 before 006 reduces merge friction (each
  plan's diff already notes this).

## Findings considered and rejected

- **Add ESLint/Prettier (would-be plan 005): rejected, no plan file written
  — superseded by the repo's own concurrent work.** Commit `1e29d93` ("Set
  up OXC tooling") added `oxlint`/`oxfmt` with
  `lint`/`lint:fix`/`fmt`/`fmt:check` scripts and an `.oxfmtrc.json` — the
  exact gap this finding targeted, just with a different (Rust-based)
  toolchain. Don't re-plan this unless OXC's tooling turns out to be
  inadequate (e.g. missing a Svelte-specific lint rule the project needs) —
  that would be a fresh finding to re-audit, not a reason to revive this one.

## Not selected this round

The advisor's original findings table also included **#1 — no automated
tests or CI** (High impact, M effort). The user chose to plan findings
2 through 7 this round and deferred #1. It remains the single highest-impact
gap in this repo (a non-trivial playback/metadata/caching state machine with
zero test coverage) and is a good candidate for the next planning pass.

Two "direction" suggestions from the original audit were not planned (by
design — direction items are options, not findings, and weren't part of the
"2-7" selection): MediaSession API integration for lock-screen controls, and
PWA installability. Worth noting: the in-progress uncommitted work in
`+page.svelte` already includes a `SELECTED_STATION_STORAGE_KEY`, meaning the
"persist last-selected station" direction item is apparently already being
built independently — check current `main` before re-suggesting it.
